import 'package:flutter/material.dart';

class InputFeed extends StatelessWidget {
  const InputFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
